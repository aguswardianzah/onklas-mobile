package id.onklas.app.pages.sekolah

import androidx.room.Dao

@Dao
interface PostDao {

//    @Insert(onConflict = OnConflictStrategy.REPLACE)
//    suspend fun insert(data: List<PostItem>)
//
//    @Query("select * from post order by time desc")
//    fun getPost(): DataSource.Factory<Long, PostItem>
}